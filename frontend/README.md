# 前端（企业安全风险评估）

Vue 3 + Element Plus + Vite。文案在 `src/locales/`（`zn` / `en` / `de`）。

## 脚本

```bash
cd frontend
npm install
npm run dev
```

或在仓库根目录：`npm run dev`（见根目录 `package.json`）。

## 路由

- `/` 首页说明
- `/erm/*` 风险评估工作台（部分页面需登录）
